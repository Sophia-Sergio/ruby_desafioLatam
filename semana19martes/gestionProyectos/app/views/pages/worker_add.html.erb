<p id="notice"><%= notice %></p>

<h1>Proyectos</h1>

<table class="table">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
      <th>Acciones</th>
    </tr>
  </thead>

  <tbody>
    <% @projects.each do |project| %>
      <tr>
        <td><%= project.name %></td>
        <td><%= project.desc %></td>
        <td><%= link_to 'Agregar a proyecto', worker_in_path(project, @worker) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>