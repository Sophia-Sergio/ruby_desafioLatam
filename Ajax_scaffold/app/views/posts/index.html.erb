<p id="notice"><%= notice %></p>

<h1>Posts</h1>
<form action="<%= posts_path %>">
  <h5>Buscar</h5>
  <input class="form-input" id="q">
</form>
<div id="posts">
  <table class="table" class="table">
    <thead>
      <tr>
        <th> Título </th>
        <th> Eliminar </th>
      </tr>
    </thead>
    <tbody id="posts-table">
      <%= render @posts %>
    </tbody>
  </table>
</div>
<h1>Nuevo Post</h1>
<%= render 'form' %>
<%= link_to 'Back', posts_path %>
<br>
<%= link_to 'New Post', new_post_path %>

<script>
$("#q").keyup(function(e){
  e.preventDefault();
  $.ajax({
    type: "GET",
    url: '<%= posts_path %>',
    data: { q : $(this).val() },
    dataType: "script",
  }).done(function(data){
    console.log('llegué');
  });
});
</script>
